# Game Assist：项目说明与后续 AI 开发指引

> 本文是后续 AI 修改本项目时的首要上下文。开始工作前应完整阅读本文，再阅读与任务相关的源码和测试。不要仅根据 `README.md` 或界面文案推断实际实现。

## 1. 项目目标

Game Assist 是一个 Windows 可见画面自动化宿主，用于自有程序、离线/单机游戏或已经获得明确授权的自动化测试。

程序通过标准桌面能力完成以下闭环：

```text
查找目标窗口
  → 截取客户区可见画面
  → 将同一帧交给所有已启用功能插件
  → 插件返回识别结果、预览叠图和动作请求
  → 宿主检查前台窗口并统一执行动作
  → 记录动作结果和释放所有残留按键
```

明确的安全边界：

- 只处理屏幕上可见的目标窗口客户区。
- 只使用标准 Windows 窗口、截图和输入 API。
- 不读取目标进程内存，不注入进程，不修改游戏文件。
- 不绕过反作弊、安全检测、权限控制或平台限制。
- 目标窗口失焦、停止、取消或异常时必须释放所有已按下按键。
- 插件不能绕过宿主直接发送输入。

## 2. 当前用户体验

桌面界面目前分为三个主区域：

1. **运行控制**：选择窗口，查看实时生命值、置信度和最近事件，启动或停止宿主。
2. **功能插件**：左侧选择已安装插件，右侧显示插件自己的设置面板并控制启停。
3. **运行日志**：显示最近的动作审计记录。

默认热键是 `F8` 启动/停止、`F12` 紧急停止。热键可在 YAML 中修改。

当前有两个可用功能插件：

- `auto_heal` / 自动喝血：使用固定 ROI 和 HSV 色段识别水平血条，连续多帧低于阈值后申请治疗按键动作，并维护每条规则自己的冷却。
- `auto_key` / 自动按键：解析受限宏 DSL，通过非阻塞状态机执行启动/定时触发、插件指标条件、键鼠动作、等待与循环；设置面板还包含前台目标窗口键鼠录制器。

自动打怪、怪物检测、选怪、OCR 和 ONNX 模型尚未实现。

## 3. 代码地图

| 路径 | 职责 |
| --- | --- |
| `src/game_assist/main.py` | CLI/UI 入口、日志和退出信号 |
| `src/game_assist/ui.py` | 主窗口、插件目录、运行状态、预览校准和日志页 |
| `src/game_assist/runner.py` | 截图循环、焦点安全、插件调度、动作执行、预览合成和审计 |
| `src/game_assist/config.py` | YAML 加载、兼容、校验和保存 |
| `src/game_assist/windows.py` | Windows 窗口枚举、客户区坐标、前台检测和激活 |
| `src/game_assist/input.py` | 按键表达式解析、安全按下/释放和取消处理 |
| `src/game_assist/health.py` | 可复用的 HSV 水平血条检测算法 |
| `src/game_assist/rules.py` | 自动喝血目前使用的连续帧和独立冷却规则计算器 |
| `src/game_assist/plugins/base.py` | 所有功能插件必须遵守的宿主协议 |
| `src/game_assist/plugins/registry.py` | 后端插件元数据和工厂注册 |
| `src/game_assist/plugins/ui_registry.py` | 插件设置面板注册 |
| `src/game_assist/plugins/auto_heal.py` | 自动喝血后端插件 |
| `src/game_assist/plugins/auto_heal_ui.py` | 自动喝血专属设置面板 |
| `src/game_assist/plugins/auto_key.py` | 自动按键 DSL 解析器和非阻塞宏状态机 |
| `src/game_assist/plugins/auto_key_ui.py` | 自动按键脚本编辑器、帮助和校验反馈 |
| `src/game_assist/recording.py` | Win32 低级键鼠录制、前台过滤、轨迹采样和 DSL 导出 |
| `config/profile.example.yaml` | 可提交的示例配置和默认值 |
| `tests/` | macOS/Linux 可运行的纯逻辑与模拟测试 |
| `.github/workflows/windows.yml` | Windows 测试与 Nuitka 打包 |
| `scripts/run.ps1` / `run-windows.cmd` | 无需激活 venv 的 Windows 自举与启动入口 |
| `scripts/build.ps1` / `build-windows.cmd` | 本地测试、Nuitka 打包及 ZIP 组装入口 |

运行产生的 `config/profile.yaml`、`debug/` 和 `logs/` 是本机数据，已经被 Git 忽略。

## 4. 插件模型

### 4.1 宿主和插件的边界

宿主拥有：

- 一次窗口截图，并把同一帧传给所有插件；
- 目标窗口和前台状态；
- 取消事件和紧急停止；
- 唯一的键盘执行器；
- 动作审计和预览叠图渲染。

插件拥有：

- 本功能需要的识别器或模型；
- 连续帧、目标锁定、冷却、状态机等运行状态；
- 本插件配置的解释与校验；
- 是否申请动作的判断；
- 本插件的预览框和状态摘要；
- 本插件的设置面板。

禁止把新功能的怪物类别、技能键、模板路径或状态机判断直接写进 `runner.py`。如果新功能需要宿主提供一种通用能力，应先抽象通用协议并为所有插件提供，而不是增加 `if plugin_id == ...` 分支。

### 4.2 核心协议

`FeaturePlugin` 包含四个生命周期方法：

- `reset_session()`：每次开始运行或重载配置时清空会话状态和冷却。
- `reset_observations()`：失焦或识别中断时清空连续帧、锁定目标等短期观察，但是否保留技能冷却由插件语义决定。
- `process_frame(frame, now)`：分析一张 BGR 客户区截图，返回 `PluginResult`。
- `mark_action_completed(request, now)`：只有动作完整发送成功后才会调用，用于开始冷却和推进状态机。

计时型插件可以额外实现 `TimedFeaturePlugin.process_tick(now, context)`。宿主在截图间隔之间按 `poll_interval_ms` 调用它。计时插件不得自行创建不可控线程或使用阻塞式 `sleep()`。

`PluginResult` 可以提供：

- `summary`：简短状态；预览使用 OpenCV 字体时应尽量使用 ASCII。
- `percent`、`confidence`、`streak`：目前保留给自动喝血和主界面兼容使用。
- `overlays`：由插件产生的 `OverlayBox`，宿主统一绘制。
- `action`：本帧最多一个 `ActionRequest`。

`ActionRequest` 只能描述动作，不能自行执行动作。`token` 是插件内部用于关联规则或状态的稳定整数。

### 4.3 注册一个插件

新增插件至少包含：

1. 在 `src/game_assist/plugins/` 中实现后端类。
2. 在 `plugins/registry.py` 注册 `PluginDescriptor`。
3. 如需图形设置，在独立文件实现设置面板并注册到 `plugins/ui_registry.py`。
4. 在 `config/profile.example.yaml` 的 `plugins.<plugin_id>` 下增加示例设置。
5. 为识别、状态转换、冷却、动作完成/取消和配置往返补充测试。
6. 更新本文“当前状态”和路线图。

未知插件设置会由配置系统保留，可通过 `config.plugin_settings(plugin_id)` 获取。插件专属设置必须放在自己的命名空间：

```yaml
plugins:
  auto_heal:
    enabled: true
  monster_combat:
    enabled: false
    detector: template
    confidence: 0.82
    attack_key: "1"
```

### 4.4 当前插件架构的已知技术债

后续 AI 不应误认为插件系统已经全部完成：

- `health_bar`、`rules` 和 `additional_rules` 仍位于 YAML 顶层，这是旧配置兼容遗留；最终应迁入 `plugins.auto_heal.settings`，但必须提供旧配置迁移和往返测试。
- 主界面的 HP、置信度字段仍通过 `runner.last_reading` 特判 `auto_heal`；未来应增加通用的插件状态卡协议。
- 多个插件同时申请动作时，宿主目前按注册顺序只执行第一个，没有显式优先级、资源锁或全局冷却。
- 任意插件抛出异常会结束整个运行器；未来应考虑插件级故障隔离，并在界面显示该插件已故障。
- 当前是编译期注册的内置插件，不会从任意目录动态导入第三方 Python 代码。
- 设置面板拥有对主 UI 宿主对象的引用；后续可收窄为明确的 UI context/protocol。

不要在没有测试和迁移方案的情况下“一次性重写”这些部分。

## 5. 自动喝血的准确语义

自动喝血插件的触发条件是：

```text
血条 ROI 有效
AND 识别置信度 >= min_confidence
AND HP 严格小于规则阈值
AND 上述条件连续满足 consecutive_frames 帧
AND 该规则自己的 cooldown 已结束
```

多个规则同时满足时选择阈值更低、更加紧急的规则。紧急规则仍在冷却时，不应退回执行更宽泛的普通规则。

只有宿主确认按键完整发送后才开始冷却。取消或失败动作必须写日志，但不能开始冷却。

HSV 主色段与所有 `hsv_ranges` 附加色段取并集；不能因存在附加色段而丢弃主色段。没有任何匹配颜色时返回 0% 和 0 置信度，以避免把“识别失败”当作可靠低血量。

## 6. 自动按键的准确语义

自动按键是基础动作插件，不是任意代码执行器。它只接受 `auto_key.py` 解析器明确定义的 DSL：

```text
macro 示例连招
trigger every 5s
when auto_heal.percent < 50 and auto_heal.confidence >= 0.8
repeat 3
press 1
wait 300ms
press CTRL+2 hold 80ms
end
```

当前语法包括：

- `macro <名称>` 到 `end` 定义一个宏，一个脚本可以有多个宏；
- `trigger start` 在本次会话条件首次满足时运行；
- `trigger every <时间>` 定时尝试运行；
- `when always` 或读取先执行插件的 `percent`、`confidence`、`streak`；
- 多个条件只能用 `and` 连接；
- `repeat 1..10000` 或 `repeat forever` 重复整个动作列表；
- `press <key> [hold <时间>]`；
- `key_down <key>` / `key_up <key>`，用于保留录制的按住时长；
- `move <x> <y>`、`mouse_down/mouse_up <button> <x> <y>`；
- `wheel/hwheel <delta> <x> <y>`；鼠标坐标相对于目标窗口客户区；
- `wait <时间>`，支持 `ms` 和 `s`。

所有等待都通过 `time.monotonic()` 状态推进，不阻塞线程。存在定时插件时宿主约每 10ms 推进一次，实际精度仍受 Windows 调度影响；同一时刻的连续动作可以在一个有界批次内完成。动作未完成时程序计数器不得前进；只有宿主调用 `mark_action_completed()` 后才能执行下一条命令。

录制器只在目标窗口处于前台时采集物理键盘、鼠标键、滚轮与按 20ms 最小间隔采样的轨迹；忽略 injected 事件，按 F10 停止，并将客户区相对坐标转换为上述 DSL。单次最多 5000 个事件。录制与回放不得绕过前台检查、F12 紧急停止、按键/鼠标键释放和审计。

不得用 `eval()`、`exec()`、Shell、PowerShell、文件包含或动态 Python 扩展脚本。需要的新逻辑必须实现为可解析、可校验、可测试的 AST/数据结构。应限制脚本大小、循环次数和时间范围，避免资源失控。

## 7. 自动打怪插件实施方案

不要一开始就同时实现模型训练、选怪、移动、技能轮转和复杂 UI。按以下阶段推进，每阶段都应可以单独验证。

### 阶段 A：离线数据与只读检测

目标：只识别，不发送任何按键。

1. 收集不同分辨率、UI 缩放、地图、亮度、怪物朝向和特效遮挡下的截图。
2. 明确要识别的是怪物本体、头顶血条、名称文本还是锁定框。
3. 固定视角且目标少时先实现模板匹配；变化明显时再使用 YOLO 并导出 ONNX。
4. 定义纯数据结构，例如 `MonsterDetection(kind, confidence, bbox)`。
5. 在预览中用 `OverlayBox` 显示检测框、类别和置信度。
6. 用静态图片建立真阳性、假阳性、空画面和遮挡测试。

验收标准：连续预览至少能稳定区分“无怪物、存在目标、多个目标”，且此阶段绝不产生 `ActionRequest`。

### 阶段 B：目标选择状态机

建议状态：

```text
IDLE
  → SEARCHING
  → TARGET_CANDIDATE
  → TARGET_LOCKED
  → COMBAT
  → LOST_TARGET / RECOVERING
  → SEARCHING
```

至少实现：

- 连续多帧确认目标，避免单帧误判；
- 目标 ID 或空间位置关联，避免每帧切换目标；
- 目标消失超时；
- 多目标选择策略，例如距离画面中心最近或指定类别优先；
- 失焦后清空锁定状态；
- 状态变化日志和预览提示。

此阶段仍可仅输出计划动作，不真正发送。

### 阶段 C：基础攻击动作

先只支持一个明确动作，例如检测到锁定目标后按一次普攻键。

- 动作通过 `ActionRequest` 交给宿主。
- 插件在 `mark_action_completed()` 后才推进状态或开始冷却。
- 设置最短动作间隔、目标确认帧数和丢失超时。
- F12 必须能在长按和动作序列中立即取消并释放键。
- 目标窗口失焦时绝不发送动作。

### 阶段 D：技能轮转

技能应是结构化配置，不要把按键顺序写死在识别函数里。每个技能至少需要：

- 名称和按键表达式；
- 优先级；
- 技能冷却；
- 施法/按住时长；
- 使用条件，例如目标类型、玩家 HP、目标是否锁定；
- 动作成功后的下一状态。

在增加多技能前，应先实现宿主级动作仲裁：动作优先级、同一帧冲突、键盘资源锁、最小全局动作间隔，以及紧急治疗是否可以抢占攻击动作。

### 阶段 E：插件设置界面

自动打怪面板建议分为：

- 插件启停和当前状态；
- 检测器类型、模型/模板路径、检测阈值和识别 ROI；
- 目标类别与选择策略；
- 状态机时间参数；
- 技能表格及其优先级、冷却和条件；
- 只识别预览/实行动作开关；
- 最近一次目标、动作和错误。

不要为每种怪物创建一个主 Tab。怪物类型应是自动打怪插件内部的数据项或规则。

## 8. 推荐后续路线图

### P0：稳定插件宿主

- [ ] 在 Win11 真实桌面完成自动喝血端到端测试。
- [ ] 为窗口、截图和键盘增加模拟接口，使整个 runner 可在非 Windows 环境集成测试。
- [ ] 增加通用插件状态协议，移除主运行器对 `auto_heal` 状态字段的特判。
- [ ] 插件异常隔离：单个插件失败时停用该插件、释放按键并记录错误，而不是让宿主静默崩溃。
- [ ] 实现动作优先级和宿主级动作仲裁。
- [ ] 为自动按键增加显式动作优先级，并定义它与自动喝血、战斗动作的抢占关系。
- [ ] 将自动喝血旧配置迁入插件命名空间，同时兼容已有 `profile.yaml`。

### P1：自动打怪只读 MVP

- [ ] 确认识别目标和提供一组真实截图。
- [ ] 实现模板匹配版 `MonsterCombatPlugin`，只输出检测框。
- [ ] 加入连续帧确认、目标丢失和静态图片回归测试。
- [ ] 提供只读预览设置面板。

### P2：安全的基础战斗

- [ ] 实现目标选择状态机。
- [ ] 只加入一个基础攻击动作并验证取消、失焦和冷却。
- [ ] 增加插件状态、动作计划和失败原因日志。
- [ ] 在 Win11 实机完成紧急停止测试。

### P3：技能系统

- [ ] 定义结构化技能配置和条件。
- [ ] 实现技能优先级、独立冷却和动作冲突处理。
- [ ] 明确自动喝血与攻击技能的抢占关系。
- [ ] 增加技能编辑 UI 和配置迁移测试。

### P4：更强识别与分发

- [ ] 数据足够后评估 YOLO/ONNX，避免无数据先引入重模型。
- [ ] 增加 OCR、状态图标或怪物血条等独立插件。
- [ ] 设计带 manifest、版本和兼容性检查的插件包。
- [ ] 若开放外部插件加载，必须先设计可信来源、权限边界和故障隔离；不能直接执行任意目录中的 Python 文件。

## 9. 测试与验证要求

常规改动至少运行：

```bash
python -m pytest -q
python -m compileall -q src tests
git diff --check
```

当前测试应持续覆盖：

- YAML 保存后可重新加载且数据相等；
- 旧配置缺少插件区时默认启用自动喝血；
- 未知插件专属设置保存后不丢失；
- ROI、HSV、阈值、冷却和按键表达式校验；
- HSV 主色段与附加色段合并；
- 严格小于阈值、连续帧、低置信度重置和规则独立冷却；
- 插件禁用后不被宿主构建；
- 动作完成后通知所属插件，取消动作不通知；
- 输入序列、组合键和异常释放。
- 自动按键脚本解析错误、条件求值、等待、有限/无限循环和动作完成推进。

任何涉及 Windows API、Tk 布局、真实截图、焦点切换或输入发送的改动，还必须在 Win11 登录桌面会话验证。GitHub Actions Windows runner 只能验证测试和打包，不能代替真实交互桌面。

建议的 Win11 冒烟流程：

1. 启动一个已授权测试窗口。
2. 在 UI 中选择目标并保存。
3. 打开自动喝血插件的只读预览，框选 ROI、取色并观察置信度。
4. 验证插件关闭时不能开始无功能运行。
5. 启用插件并触发一次低血量动作。
6. 验证日志包含插件、规则、HP、阈值、置信度、按键和结果。
7. 动作过程中按 F12，确认所有按键释放。
8. 运行中切走前台，确认暂停且不发送动作；切回后恢复。

## 10. 后续 AI 的工作规则

1. 开始前先查看 `git status`，不得覆盖用户已有或其他任务产生的改动。
2. 先确认需求属于宿主通用能力还是独立功能插件；业务功能默认放入插件。
3. 诊断和修改必须以当前源码为准，不把路线图中的未完成功能描述成已实现。
4. 保持旧配置兼容；新增字段必须有默认值、校验、保存往返测试和示例配置。
5. 不允许插件直接调用 `SendInput`，所有动作必须走 `ActionRequest`。
6. 不用 `time.sleep()` 实现不可取消等待；使用宿主取消事件或基于 `monotonic()` 的状态机。
7. 冷却和状态时间使用 `time.monotonic()`，日志时间可以使用墙上时间。
8. 识别函数不得混入直接按键逻辑；识别结果、规则决策和动作执行保持分离。
9. 先实现只读识别预览和离线测试，再开启真实动作。
10. 新插件必须提供禁用路径；禁用后不得处理帧、加载重模型或请求动作。
11. 异常路径必须释放按键并产生可理解的状态或日志。
12. 不新增任何规避检测、绕过安全机制、内存读取或进程注入能力。

## 11. 完成标准

一个功能不能因为“代码已经写完”就标记完成。至少满足：

- 插件边界正确，主运行器没有新增业务特判；
- 配置可加载、校验、保存并兼容旧版本；
- 有正常、边界、取消、失焦和错误路径测试；
- 预览能解释识别结果和置信度；
- 所有动作都可取消并留下审计记录；
- 自动化测试通过；
- 涉及 Windows 交互时完成实机冒烟；
- `README.md`、本文和 `FEATURES.md` 与真实行为一致。

如果缺少真实截图、模型、目标类别、按键映射或验收规则，AI 应先完成不依赖这些输入的宿主与测试工作，然后明确列出阻塞项，不得凭空假设游戏行为。
