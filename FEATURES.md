# 功能清单与开发状态

最后更新：2026-09-09

## 已完成

### 自动化核心

- [x] **安全边界**：仅使用可见窗口截图与标准 Windows 输入 API；不读取进程内存、不注入进程、不尝试绕过反作弊或安全机制。
- [x] **窗口绑定**：按可见窗口标题匹配目标；运行时要求目标窗口保持前台。
- [x] **窗口选择器**：桌面界面可刷新当前可见窗口，显示窗口标题与 PID，选择后自动写入目标窗口标题。
- [x] **客户区截图**：仅截取目标窗口的客户区，所有 ROI 坐标以客户区左上角为原点。
- [x] **血条检测**：在固定 ROI 内按 HSV 颜色掩码与横向填充列比例计算血量百分比和置信度。
- [x] **治疗规则**：血量连续多帧低于阈值且置信度足够时，按一次配置的治疗键；每条规则独立冷却，紧急规则优先。
- [x] **触发审计**：每次按键记录 HP、严格小于的阈值、置信度、规则名、实际按键和发送结果；界面和日志文件均可查看。
- [x] **可取消执行**：轮询、按键等待均支持取消；停止、失焦或异常时会释放已按下的键盘键和鼠标键。失焦后暂停，目标窗口回到前台时自动继续。
- [x] **识别节流**：截图与识别间隔可配置，预览和自动化不会以窗口检查频率重复执行昂贵判断。
- [x] **全局热键**：默认 `F8` 启动/停止，`F12` 紧急停止；可在 YAML 配置中修改。

### 桌面界面

- [x] **Windows 控制台**：默认入口启动深色 Tkinter 界面；保留 `--cli` 参数使用原终端模式。
- [x] **插件中心**：按运行控制、功能插件和运行日志拆分界面；自动喝血可独立启停和配置。
- [x] **功能插件协议**：插件独立持有识别、规则、状态、冷却和叠图，宿主统一负责截图、安全动作执行与审计。
- [x] **自动按键插件**：支持启动/定时触发、插件指标条件、键盘与鼠标动作、等待、组合键、有限循环和无限循环；使用非阻塞状态机执行。
- [x] **安全脚本编辑器**：自动按键插件拥有独立脚本面板和语法校验，不执行 Python、Shell 或任意系统命令。
- [x] **键鼠录制器**：仅在目标窗口前台时录制物理键盘、鼠标按键、滚轮和采样轨迹，按 F10 停止并转换为可编辑宏；忽略注入事件并限制单次事件量。
- [x] **运行控制**：提供开始、停止和当前运行状态显示。
- [x] **常用配置编辑**：可编辑目标窗口标题、治疗阈值、治疗按键和“保存调试截图”开关。
- [x] **本地配置保存**：配置保存至 `config/profile.yaml`；首次启动若不存在该文件，会由 `profile.example.yaml` 自动创建。
- [x] **调试截图**：可按秒保存客户区原图和识别叠图，用于测量 ROI 与调整 HSV 参数。

### 工程、测试与交付

- [x] **YAML 配置**：窗口、热键、截图、HSV/ROI、置信度和规则均可配置。
- [x] **单元测试**：已覆盖水平红色血条的识别，以及配置保存后可重新加载。
- [x] **Git 忽略规则**：忽略本机 `profile.yaml`、调试截图、虚拟环境和 Python 生成文件。
- [x] **Windows GitHub Actions**：在 push、PR 或手动触发时运行 `pytest`、使用 Nuitka 打包，并上传 `game-assist.exe` 产物。
- [x] **非交互式 Nuitka 构建修复**：允许 CI 下载并缓存 Dependency Walker，避免 GitHub Actions 因交互确认失败。

## 当前限制

- [!] **窗口 PID 不是永久标识**：同名窗口可通过 PID 区分，但目标程序重启后 PID 会变化，需要重新选择。
- [!] **ROI 仍需手工输入 YAML**：已提供带 ROI 的 debug 叠图，但可视化框选与 HSV 取色器尚未完成。
- [!] **规则来源仍限血条百分比**：已支持多条规则、组合键/序列和长按，但尚未加入蓝量、异常状态或目标丢失检测。
- [x] **实时识别预览**：可显示客户区画面、健康条 ROI、HP、置信度、连续帧计数和最近动作；debug 模式会额外写入 `debug/` 目录。
- [x] **可视化校准**：预览提供独立的 ROI 框选和 HSV 取色模式，支持放大、缩小、适应窗口与滚动查看。
- [!] **Windows 专用集成能力**：窗口枚举、前台检查、真实截图、全局热键和键盘输入需要 Windows 交互式桌面会话。
- [!] **GitHub Actions 不能替代实机测试**：托管 runner 可测试算法与打包，但没有真实游戏窗口，不能验证焦点、热键、截图和按键效果。

## 建议的下一步

### 优先级 P0：可用性与测试

- [ ] 在 Windows 实机完成一次端到端验证：选择窗口、生成调试截图、校准 ROI/HSV、确认低血量动作与紧急停止。
- [x] 在界面中显示实时血量、置信度、最近一次动作和触发日志。
- [x] 增加“截图预览 + 鼠标框选 ROI + HSV 取色”校准工具，减少手工编辑 YAML 的成本。
- [ ] 为窗口选择保存更稳定的标识（PID/进程路径/窗口句柄策略），并对同名窗口要求用户确认。

### 优先级 P1：识别与自动化能力

- [ ] 支持多段 HSV 范围、模板匹配或 OCR，以适应不同 HUD 与状态提示。
- [ ] 支持多个规则：补血、补蓝、异常状态、目标丢失等，并提供优先级和冷却配置。
- [x] 增加检测防抖、连续帧确认和动作审计，降低误识别导致的误触发。
- [x] 增加可配置的按键组合、长按、动作序列和更完整的按键合法性校验。

### 优先级 P2：工程化

- [ ] 抽象截图、窗口和键盘接口，加入模拟实现，使 macOS 可以测试完整规则流程而不触发 Windows API。
- [ ] 扩展单元测试：空 ROI、低置信度、冷却、失焦、取消和同名窗口等场景。
- [ ] 将 Windows 实机冒烟测试写成明确的测试步骤或脚本，并在每次发布前执行。
- [ ] 为发布产物添加版本号、图标、变更日志和可选的代码签名。

## 测试分工

| 环境 | 应测试内容 | 不适合测试的内容 |
| --- | --- | --- |
| macOS | 配置、HSV 识别、规则逻辑、单元测试 | Windows 窗口、真实截图、全局热键、键盘输入 |
| GitHub Actions Windows | Python 依赖、单元测试、Nuitka 打包 | 真实目标程序与交互式桌面自动化 |
| Win11 实机登录会话 | 完整 UI、窗口选择、截图校准、热键和按键行为 | 无 |
